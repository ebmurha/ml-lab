# Tests

Tests cover API pagination, feature timing, missing-data handling, chronological splits, baseline inference and deployed boosted-tree inference. Training also verifies deterministic repeated predictions and records peak memory.
