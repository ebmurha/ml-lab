# Artifacts

Training writes `model-0.1.0.joblib` here. Generated model artifacts are ignored by Git; regenerate one with `python -m credit_default_risk.train` before running or building the service.
